const LoginScreen = () => {
    return (
        // using <> is just like using <React.Fragment>
        <>
            {/* <TextInput />
            <TextInput />
            <Button>Login</Button> */}
        </>
        // using </> is just like using </React.Fragment>
    );
};